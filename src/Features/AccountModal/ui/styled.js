import styled from "styled-components";

import { PanelContainer } from "@shared/styles/panel-list";

const Container = styled(PanelContainer)`
    flex: 1;
    min-height: 0;
`;

const Styled = {
    Container,
};

export default Styled;
